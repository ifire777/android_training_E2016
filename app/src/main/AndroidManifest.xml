<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.mrkip.apps.epamandroidtraining">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".IntroActivity"
            android:theme = "@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.NoActionBar"/>

        <activity
            android:name=".Page1Activity"
            android:launchMode="singleInstance"/>
        <activity
            android:name=".Page2Activity"
            android:launchMode="singleInstance"/>
        <activity
            android:name=".Page3Activity"
            android:launchMode="singleInstance"/>
        <activity android:name=".statLayuot.Fb1Activity"/>
        <activity android:name=".statLayuot.In1Activity" >

        </activity>

    </application>

</manifest>